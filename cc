cc

The cat command is a Linux command used to concatenate files and display their contents on the terminal.

Syntax:

css
Copy code
cat [options] [file(s)]
Example usage:

bash
Copy code
cat file1.txt
This command will display the contents of file1.txt on the terminal.

Options:

-n: displays the line number before each line of the file.
-b: displays the line number before each non-blank line of the file.
-s: replaces multiple consecutive blank lines with a single blank line.
-E: displays a $ character at the end of each line to denote the end of the line.
-T: displays tabs as ^I and other non-printable characters in a visible way.


